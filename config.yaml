kafka:
  address: ${ZG_KAFKA_ADDRESS}
  group_id: nosql_repo
  user: guest
  password: guest
  topic: processing_1

dbs:
  mongodbs:
    - ${ZG_MONGODB_URL_1}
    - ${ZG_MONGODB_URL_2}

redis:
  address: ${ZG_REDIS_URL}
  db: ${ZG_REDIS_DB}

logstash:
  url: ${LOGSTASH_URL}

tracer:
  url: ${TRACER_URL}
  enabled: true